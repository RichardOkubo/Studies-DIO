/*
 * Copyright 2021 Richard Okubo
 *
 * Aula 5 - Operadores comparativos
 */

fun main() {
    /*
        a > b, a < b, a >= b, a <= b, a == b ou a.equals(b), a != b ou !a.equals(b)
        a.compareto(b) // Retorna -1 se a < b, 0 se a == b e 1 se a > b
     */
}
