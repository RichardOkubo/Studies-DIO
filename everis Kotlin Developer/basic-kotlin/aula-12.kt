/*
 * Copyright 2021 Richard Okubo
 *
 * Aula 12 - Estruturas de controle
 */

fun main() {
    /*
    IF/ELSE:

        if (expression) {
            ...
        } else if (expression) {
            ...
        } else {
            ...
        }

    WHEN:

        when {
            case1 -> { ... }
            case2 -> { ... }
            ...
            caseN -> { ... }
            else -> { ... }
        }

    ELVIS OPERATOR:

        val a: Int? = null; var number = a ?: 0
     */
}
